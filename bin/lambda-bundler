#!/usr/bin/env node

const args = require('minimist')(process.argv.slice(2));

console.log(args);

require('../')(args)
  .then((result) => {
    console.log(result);
  })
  .catch((err) => {
    console.log(err.stack);
    process.exit(1);
  });
